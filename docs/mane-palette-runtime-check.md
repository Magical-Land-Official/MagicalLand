# 鬃毛与尾巴调色：游戏验收

2026-09-07，调色功能验收通过；范围不含完整光照或动作矩阵。

## 环境与范围

隔离测试副本，Minecraft 1.20.1 / Fabric / GeckoLib 4.8.3，Java 25.0.4。测试副本的 71 个源文件、资源及构建配置与正式仓库逐文件核对；使用已有离线依赖运行 `runClient`，两次均正常退出、任务成功。未制作发布 JAR，未修改正式玩家存档。

## 已验证

- 在已有测试世界打开外观界面，从原白色配置开始，前发改为 `#D16B6B` 后，后发、尾巴同时变色，自动阴影和高光同步变化，身体颜色保持不变。
- 解锁前发阴影、高光，使用对比测试色（紫色阴影 `#7758BA`、偏黄高光 `#D1D376`），确认色阶作用于实际模型，并同步到仍联动的后发和尾巴。
- 解锁后发、尾巴时保留当时可见配色和手动色阶；随后后发改为 `#6BD1BA`、尾巴改为 `#D1B06B`，三处主色互不覆盖。
- 点击“高光/阴影恢复自动”后，三处各自恢复自动色阶，独立主色与部位解锁状态不被合并。
- 后发重新锁定后立即恢复跟随前发，尾巴仍保持独立金色。
- 返回第三人称游戏画面，玩家模型正确显示红色鬃毛和金色尾巴。
- 正常关闭客户端后重新启动，后发联动锁、尾巴独立状态、已保存颜色与自动色阶均正确加载；检查完正常退出。
- 未出现 `Body palette unavailable` / `Mane palette unavailable` 调色回退日志或新增调色异常。原有 GeckoLib 几何版本、Java/LWJGL 及离线 Realms 提示仍存在。

## 验收截图

三处独立主色，分别使用自动阴影和高光：

![前发红色、后发青绿色、尾巴金色](images/mane-palette/three-colors-auto.png)

重启后仍保持后发联动、尾巴独立，色阶锁状态正确：

![重启后锁状态与配色](images/mane-palette/restart-locks.png)

## 非阻塞事项与未测范围

当时发现切换锁后列表回到顶部，后续修复见[颜色菜单验收记录](color-submenus-runtime-check.md)。本轮未重测多种光照/所有姿势，也未验证双客户端联机、Java 17 运行时、其他渲染器或资源热重载。

历史实验草案、原文件备份和完整原始日志继续留在本地 `Resources/PolishLab_20260907/` 与任务测试输出目录，不作为正式模型资源发布。
